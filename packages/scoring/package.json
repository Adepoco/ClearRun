{
  "name": "@clearrun/scoring",
  "version": "1.1.0",
  "description": "ClearRun UI mapping for honesty verdicts (OPEN). Numeric scoring formula lives in the closed kernel.",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@clearrun/core-types": "file:../core-types"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
